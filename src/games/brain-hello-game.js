import { gameFlow } from '../';

const brainHelloGame = () => {
  const gameFunction = () => {
    const rules = '';
    return { rules };
  };
  gameFlow(gameFunction);
};

export default brainHelloGame;
